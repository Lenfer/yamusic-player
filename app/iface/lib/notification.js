'use strict';

var ipc = require('electron').ipcMain;

module.exports = function(browserWindow) {
	// @todo ...
	/*ipc.on('api:emitter:track', function(event, track) {
		console.log(track)
	});*/
};

